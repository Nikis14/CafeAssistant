# Provider SDKs (only needed for the providers you actually use)
anthropic>=0.40.0
openai>=2.0.0
google-genai>=1.0.0
mistralai>=2.0.0

# LangChain core
langchain>=1.0.0
langchain-core>=1.0.0

# LangGraph (orchestrator + prebuilt ReAct agent)
langgraph>=0.6.0

# LiteLLM bridge for provider switching from a single ChatLiteLLM instance
langchain-litellm>=0.2.0
litellm>=1.50.0

# Web search via Tavily (Phase 4)
tavily-python>=0.5.0

# Vector store for episodic memory (Phase 3)
chromadb>=1.5.0
langchain-chroma>=1.1.0
# Keep this on the 1.39 line: chroma's telemetry stack currently resolves
# through opentelemetry-semantic-conventions 0.60b1, which conflicts with 1.40+.
opentelemetry-api>=1.39,<1.40
# HuggingFace sentence-transformers for production episodic embeddings.
# Tests bypass via TASTE_AGENT_FAKE_EMBEDDING=1.
langchain-huggingface>=1.2.0
sentence-transformers>=3.0.0

# UI
gradio>=4.40.0
playwright>=1.52.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0.1

# Dev (also installable via `pip install -e .[dev]` once we add extras)
ruff>=0.6.0
pytest>=8.0.0
